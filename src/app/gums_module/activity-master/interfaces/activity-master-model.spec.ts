import { ActivityMasterModel } from './activity-master-model';

describe('ActivityMasterModel', () => {
  it('should create an instance', () => {
    expect(new ActivityMasterModel()).toBeTruthy();
  });
});
