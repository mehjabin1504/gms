import { ComponentWiseActivityMappingModel } from './component-wise-activity-mapping-model';

describe('ComponentWiseActivityMappingModel', () => {
  it('should create an instance', () => {
    expect(new ComponentWiseActivityMappingModel()).toBeTruthy();
  });
});
